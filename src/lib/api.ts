/**
 * Remote API helpers (REST, CMS, etc.). This portfolio uses static data in
 * `@/data/projects` instead of runtime fetch — add client-safe or server-only
 * fetch wrappers here when you integrate a backend.
 */
